const express = require("express");
const router = express.Router();

// This is a placeholder file to fix the dependency issue
// The actual auth routes are handled in userRoutes.js

module.exports = router;
